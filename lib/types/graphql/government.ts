export interface Ministry {
  id: string;
  name: string;
  // Add other fields as needed
}

export interface Official {
  id: string;
  name: string;
  position: string;
  // Add other fields as needed
}
